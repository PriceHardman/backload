<h1>Listing hold_templates</h1>

<table id="thisTable">
  <tr>
    <th><h2>Hold</h2></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @hold_templates.each do |hold_template| %>
  <tr>
    <td><h3><%= hold_template[:vessel]+" "+hold_template[:hold]%></h3></td>
    <td><%= link_to 'Show', hold_template %></td>
    <td><%= link_to 'Edit', edit_hold_template_path(hold_template) %></td>
    <td><%= link_to 'Destroy', hold_template, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Hold template', new_hold_template_path %>
