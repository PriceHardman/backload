$('#update_selectable_cells_form').remove();
$('#edit_selectable_cells_link').show();
$('#grid').attr('data-action','show');

//update the $hold var with the new data:
$('#hold_var').html('$hold = <%= raw @hold.to_json %>;');

//unbind the hover and click events for editing the selectable cells:
$('#grid td').unbind('mouseenter').unbind('mouseleave');

$('#grid td').unbind('click');

$('.hover').removeClass('hover');
